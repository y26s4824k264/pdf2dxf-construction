# 2.0.0rc20

- 修复 `make_valid` 返回含 Polygon 和退化线段的 GeometryCollection 时整组填充被丢弃的问题；递归提取有效面，保留源轮廓方向、孔洞和独立描边。
- 新增 17 项回归，覆盖两种填充规则、正反方向、孔洞、多面、零面积退化、PDF→已保存 HATCH、描边和裁剪。
- 84 个真实字体样例完整恢复从 61 提高到 62，补齐 Jigmo 扩展 I 的一个填充样例；22 个样例仍部分恢复或未确认，字库与匹配门槛不变。
- 22 份 BIM 图纸复测：独立绕数核验 9,895 条修复填充记录，保存结果增加 14,345 个 HATCH（包括块内实体）。非填充实体、文字和比例结果与 rc19 对齐，工程质量降级仍明确保留。
- 更新中英 README、项目说明与可复核证据。详见 docs/VALIDATION.md。

# 2.0.0rc19

- 修复 PDF 数值保存误差使轮廓顶点跨越栅格取整边界、导致思源宋体及遍黑体部分英文字母缺失的问题；有限 3/4/5 位取整变体仍要求精确字库掩码与拓扑，不改变 DXF 几何。
- 跨所有数值变体和字库统一拒绝标签冲突；保留字体锁定、重复/相邻一致性、重叠歧义和工程比例门槛，报告新增每字取整精度与匹配证据。
- 新增 8 项回归，覆盖描边/填充边界、超界变形拒绝、同库/跨库冲突、原始几何保留与幂等；84 个真实字体样例完整恢复由 57 提升到 61，仍有 23 个部分恢复或未确认，不将覆盖数当作准确率。
- 复用 rc18 字库资源与许可证，更新中英 README、项目说明和本轮验证证据。详细实测及工程限制见 docs/VALIDATION.md。

# 2.0.0rc18

- 提供 10 个开源静态字体字库，分成常用中英文与扩展汉字两个独立资源包；固定官方来源版本、SHA256、许可证和版权文本，主 wheel 不含字体或字库。
- 与 Unicode 17 官方字符表逐码点核对，Jigmo 三个字库并集覆盖所支持基本区、A–J、兼容区全部 102,998 个已分配汉字码点；不把模板覆盖宣称为任意字体识别率。
- 新增 `--outline-font-bundle`、`font-catalog inspect-bundle`、Python 字库包校验接口，以及支持离线缓存和已完成字库复用的可复现构建工具。
- 修复精确完整字形内部的标点片段导致整字误拒绝的问题，例如思源黑体 `i` 的竖画被识别成竖排破折号；竞争的字母/汉字与交叉重叠仍保持歧义，字体锁定和连续成行条件保留。
- 新增 31 项字库分发、文件保护、完整性、离线 TEXT 恢复和轮廓重叠回归；完整测试及实图证据见 docs/VALIDATION.md，真实字体的完整/部分/未确认结果逐项公开。

# 2.0.0rc17

- 补齐 A–Z / a–z 全部 52 个英文字母的纯英文轮廓恢复；相邻四个不同字母可独立确认字体，保留大小写，并支持中英混排的正常字高差异。
- 修复补充平面汉字已匹配却被输出检查拦截的问题；汉字基本区、扩展 A–J 和兼容汉字使用统一 Unicode 检查，保留既有单码位 NFKC 标准化。
- 精确拓扑/掩码匹配增加逐标签宽高比核验，排除同掩码而比例不同的候选；同形歧义和孤立匹配继续保留几何。
- 支持读取持久化 DXF 中纯填充字形的闭合 HATCH 折线边界；同源描边避免重复，非平面/开放/含 bulge 的填充保持原样。默认保留源填充，重复恢复幂等。
- 字库保留小数曲线采样比值，默认由 5 组扩展到 17 组，补齐常用字号贝塞尔曲线的采样匹配；rc17 继续读取旧字库，新采样字库要求 rc17+。
- 新增 `--charset english`、52 字母覆盖/缺字统计和各汉字区段映射统计；中英双语说明明确字体、扫描页和工程比例的验证边界。
- 新增完整真实英文字母、纯填充曲线、Unicode 汉字区段、混排、旧字库兼容、填充保留/幂等和拒绝歧义场景的回归。完整测试与实图结果见 docs/VALIDATION.md。

# 2.0.0rc16

- GitHub 首次公开发行：维护者授权采用 AGPL-3.0-only，补齐 LICENSE / NOTICE、包许可元数据、来源声明、中英双语 README 与项目链接。
- 发布包检查要求归档中实际包含许可及第三方声明，导出快照保留维护者声明的发布状态；新增 2 项发布回归，连同 Windows 编码回归共 226 项。
- 修复 Windows 非 UTF-8 日志导致已恢复中文的任务失败：内部 JSON 统一按 UTF-8 读取，worker 日志使用无损 JSON 转义，CLI 在旧编码输出流中回退到可解码的 JSON；增加中文路径和 cp1252 端到端回归。
- 输入预检、修复、页码读取和文件哈希移入受监督子进程，补齐原来发生在资源监控之前的解析路径；报告新增 input_validation 及资源证据。
- 非 PDF 和加密 PDF 给出明确错误；qpdf / mutool 超时或启动失败可继续后续修复，最后的 PyMuPDF 重写避免整份 PDF 的额外内存副本。
- 输出锁支持有界等待和取消；Ctrl+C 返回 130，终止活动子进程树，批处理先发出取消信号再等待工作线程退出，防止中断后仍处理队列。
- 修复无系统字体的 Linux 环境 R12 MTEXT 无法拆分的问题；可选 Matplotlib 提供度量，处理 ezdxf 失效默认字体缓存，并以 R12_FONT_METRICS_FALLBACK 明确外观近似。通用 DXF 和中文识别规则不变。
- 提高 Pillow / FontTools 安全版本下限，精确指定 OpenCV 最低已发行 wheel 版本；对最低直接依赖和实际解析环境执行已知漏洞检查。
- 发布检查新增 wheel / sdist 内容校验和独立许可状态检查；CI 固定 Action 提交，验证安装包不读取工作区源码。
- GitHub CI 已在 Ubuntu Python 3.10/3.13、macOS Python 3.12、Windows Python 3.12 上分别通过 226 项源码与 226 项隔离安装包回归；发布前本地 222 项基线独立记录在 docs/VALIDATION.md。
- 重新验证 22 份内部 PDF 的转换、保存后 DXF、中文文本、比例与输入预检证据；保留未通过的质量状态，不放宽工程门槛。

# 2.0.0rc15

- 修复 batch 将质量降级当成退出成功；空输入、非法清单拒绝执行，重复规范路径只转换一次；regress 隔离单文件异常。
- 保护输入、字库、输出 DXF、分页/R12 图纸和报告，识别符号链接及硬链接别名；汇总报告和 CLI JSON 使用原子写入。
- 页码在参数解析阶段验证，显式范围有界，避免巨大范围提前分配内存。
- 字库在创建 NumPy 数组前检查头部、类型、形状和实际长度，按声明的合法维度限制读取；无效清单、计数、容差和 ZIP 返回可解释错误。
- 添加独立发布白名单导出器、文件哈希清单、安装后测试工具、跨平台 CI、贡献说明和第三方来源待办；源码包排除内部证据、图纸截图、原始路径和临时环境。
- 源码与隔离 wheel 各 204 项测试通过；22 份实图转换失败 0、全部保留 degraded 状态，已保存 DXF 文字、比例和 SHA256 逐份复核。
- 项目许可证和部分模板再分发授权暂待维护者决定，本轮仅准备本地发布目录。

# 2.0.0rc14

- 新增任意 OpenType 轮廓字体字库构建器，支持 TTF、OTF、TTC 和 OTC face；`font-catalog faces/build/inspect` 将字体映射持久化为可校验的 `.p2dfont`，转换运行时不再读取字体文件。
- 默认中文字集按 Unicode 17.0 覆盖统一汉字扩展 A–J、兼容汉字、部首、笔画、注音符号和 CJK 标点/符号；只收入字体实际映射且具有二维轮廓的字符，另可用 `--charset all` 构建全部 Unicode 映射。
- 字库保存 Unicode 码位、闭合拓扑、实体数、56×56 归一化掩码和 5 组曲线离散指纹，并逐数组校验大小、类型、SHA256、全数据集哈希、来源字体 SHA256 和复算后的字库身份；重复归档成员、损坏或不兼容字库明确拒绝。
- 同一字体须由至少 3 个相邻且互不相同的汉字精确命中，或由 3 个内置人工审核汉字锚点命中后才能锁定。锁定后才发布其他字符；跨字体冲突、同形异字、重叠切分、孤立候选和未知轮廓保持几何。
- 修复 PDF 数值序列化在半像素边界造成的精确指纹漏字；字库与 DXF 运行时统一使用 1e-4 栅格像素量化。新增真实矢量 PDF 端到端回归，在删除源 TTF 后仍可把“中文国图”完整恢复为一个 `TEXT`。
- Songti SC Light 实测生成 29,901 个模板，其中 29,224 个汉字模板，字库为 8,257,182 bytes；纯 DXF 自锁恢复“中文国图”4/4。另验证 TrueType、CFF OTF 和 8-face TTC 构建与重载。
- 使用最终 Unicode 17.0 字库重跑 内部 22 份图纸样本 的全部 22 份 PDF，转换失败 0、保存后审计错误/修复 0。该宋体与实图轮廓字体不匹配，41 个孤立精确候选均未达到锁定门槛，外部字库发布 0 字；内置结果保持 238 条 `TEXT`、1035 个字符。
- 比例结果保持 9 页 `calibrated`、6 页 `declared_approximate`、6 页 `paper`、1 页 `unknown`；未放宽尺寸误差或工程比例门槛。
- 源码与全新隔离 wheel 各 173 项测试通过，`pip check` 通过；wheel 从 `site-packages` 导入并用最终外部字库完成真实 PDF 冒烟，包内不携带外部字体或 `.p2dfont`。

# 2.0.0rc13

- 将轮廓中文恢复改为纯 DXF 算法：先保存纸面 DXF，再按来源 XDATA、完整连续笔画、闭合拓扑、点数和归一化几何指纹精确匹配；运行阶段不读取 PDF、不调用 OCR。
- 内置 101 个带来源标签证据的完整字形模板、覆盖 63 个字符；93 个模板须在至少两个不同源 PDF 中完全一致，8 个仅允许组成经独立工程字典交叉确认的完整 `1:60` / `1:90`。
- 只发布至少两个连续汉字或完整 `1:n`；已确认文字写入 `PDF_TEXT_RECOVERED_NOOCR`，原轮廓与相关填充默认移入关闭的 `PDF_OUTLINE_BACKUP`，未知和孤立候选保持几何。
- 字形窗口改用持久化 PDF 源绘制序号连续性，避免 HATCH 等中间图元打断文字；宽标点按实际相对字高分组，补全题栏中的“注册师印章、出图专用章、工程主持人、子项名称、1-1剖面图”等文本。
- 修复 `keep` 策略重复运行会重复插入 `TEXT`；现在按 `PDF2DXF_GLYPH` 中的恢复位置幂等处理，并保留用户对已恢复文字的手工修改。
- 删除 RapidOCR/ONNX 依赖以及旧 `outline_chinese.py`、`path_text.py` 重复入口；worker 只保留基础 PDF→DXF 与持久化 DXF 文字恢复两个阶段。
- 删除唯一旧入口已关闭的 Tesseract/PDF 字形聚合/V15 尺寸推断链，共 14 个死模块和 FontTools 直接依赖；旧 `include_native_dimensions=true` 现在显式拒绝，避免静默回退到 OCR。
- 删除仓库零调用的 `convert_paper_dxf` 图框/多视口入口及其仿射导出、视口曲线裁剪、metadata/finalize、重复内部验证共 9 个实现模块和 4 个旧测试；公共接口不再列出 `mode=split`，并移除该死链独占的 `scikit-learn` 依赖。
- 内部 22 份图纸样本 的 22 份图纸全部重新转换，失败 0；恢复 238 条 `TEXT`、1035 个字符，最终 DXF 重开、XDATA、原轮廓备份、文件哈希和比例范围逐份核验通过。
- 最终代码连续两轮的 22 个 DXF SHA256 逐一相同；比例声明去重后不再因原生文本和恢复文本同时存在而重复报告。
- 源码与清理后 wheel 在两个全新 Python 3.12 环境中各 160 项测试通过；wheel 仅含 43 个文件，旧模块不存在，`pip check` 通过。

# 2.0.0rc12

- 新增可选的轮廓中文恢复：RapidOCR 以 180/280 DPI 双整页结果、480 DPI 局部复核和 PDF 绘制序号到 DXF XDATA 映射共同确认，才写入可编辑 `TEXT`。
- 已确认文字写入 `PDF_TEXT_RECOVERED_OCR`；对应原轮廓默认移入关闭的 `PDF_OUTLINE_BACKUP`，未确认内容保持原几何。数字歧义字符和低置信单次识别不写入文本。
- OCR 整页与局部阶段使用短进程、分块识别和单线程推理，修复密集表格页累计内存超限；缺少可选依赖时 `auto` 保留轮廓，`required` 明确失败。
- 新增 `scale_mode=declared`：仅在没有更高等级尺寸证据、仅有一个独立题栏比例且无冲突时等比换算，并标记 `declared_approximate`、`engineering_scale_confirmed=false`、`model_ready=false`。
- 表格、目录和建筑说明通过文件名及已恢复内容保持纸空间；材料配合比、多个比例或尺寸冲突不能触发近似全页缩放。
- `convert`、`batch`、`regress` 均可传递 `--outline-chinese` 和比例/资源参数。独立包不新增图框拆分，继续由 dtai-base 现有 DrawingIR 流程处理图框。
- 源码与隔离 wheel 各 157 项测试通过；22 份当前 BIM 图纸全部生成 DXF，共确认 341 个可编辑文字，保存后完整性复核 22/22 通过。

# 2.0.0rc11

- 保留 PDF `ImageMask` 的实际绘制颜色、透明度与每次放置，修复无颜色空间的模板图像被跳过。
- CMYK 图像转为 PNG 兼容 RGB，合成 `/SMask` 保留透明度；可见图像提取失败会阻止几何质量门槛。
- 兼容图像与软蒙版采样尺寸不同的 PDF；默认单页内存上限调至 2560 MiB，覆盖已验证的大型施工图页面。
- 只跳过经页面 bbox 交集证明完全不可见的媒体，记录跳过数量；消除页面外内联图像的无效栅格回退警告。
- 后端警告去重；字形匹配先排除不可能通过阈值的候选，保持结果一致并减少分位数计算。
- 新增图像蒙版、CMYK、透明图、`/Matte` 与异尺寸软蒙版、页面外媒体及图形门槛回归，当前共 141 项。

# 2.0.0rc10

- R12 曲线容差按 mm/cm/m/inch 换算到同一物理 0.05 mm，修复米制曲线过粗；完整展开旋转阵列，先归并零间距轴。
- 尺寸证据新增源尺寸线、界线和文字快照，按最终块变换还原纸面坐标复核；删除或修改可见图元不能只靠隐藏 DIMENSION 过关。
- 拒绝非有限尺寸阈值和缺少独立证据的旧信任摘要；空 DXF 和 R12 兼容输出不能误标为 model_ready。
- validate 自动读取转换报告、有效请求、单位和源证据并核对文件 SHA256；输出/JSON 路径冲突被拒绝，避免覆盖输入和报告自哈希失效。
- 文字去重覆盖模型/图纸空间和普通块定义，保留跨参照的正常文字；修复默认整图收块后重复文字未隐藏。
- 新增 27 项回归，当前共 134 项。原 PDF 回查覆盖全部 121 条超限尺寸的界线/尺寸线；逐页计算统一比例的可行区间上界，保留源图真实误差。

# 2.0.0rc9

- 从模型空间追踪 DIMENSION 的块参照位置，修复块输出换算厘米、米、英寸后的尺寸误判；支持嵌套基点、旋转、缩放及阵列。
- 输出证据检查最终三维端点和唯一放置，拒绝已删除、移位、转向或重复的块参照；排除未引用块中的标注。
- 限制块遍历深度与实例数，明确报告循环、缺失参照及无法测量的尺寸；角度值不再随长度单位换算。
- R12 保留 TEXT 对齐点、FIT/ALIGNED、倾斜和镜像；MTEXT 使用布局解析后降级，修复附着位置与旋转行距。移除旧的逐行固定偏移实现。
- 命令行帮助和 --version 统一读取 version.py，删除写死的 rc5 版本号。
- 新增 26 项回归；源码和隔离 wheel 各 107 项通过，重新验证 11 页已有 DXF，并完成结构 PDF 的毫米/米完整转换及厘米/英寸导出复核。

# 2.0.0rc8

- 将尺寸链分析整理到独立 `calibration/chains.py`；按分尺寸图层、基线和总分尺寸间距限制关联，修复跨位置拼接投影标注。
- 搜索完整几何路径并识别重叠分叉，不再取遇到的第一条链或按标签和选择路径；搜索未完成时保留待确认。
- 支持真实图纸中 17、24 段等长尺寸链，保留 64 段 / 2000 次搜索上限；输出验证重新构建局部尺寸链。
- 补齐带明确单位的原生小数及单个数字尺寸，统一换算毫米；修复恰为纸面 0.5 mm 的尺寸被边界条件排除。
- 新增跨位置、重叠、跨层、搜索上限、真实长链、单位解析及持久化 DXF 测量回归；保留原始比例误差和 0.2% 门槛。

# 2.0.0rc7

- 修复字高/基线分桶导致的数字截断、跳字及窗编号前缀丢失；使用空间邻接与歧义保留。
- 修复长引线穿过闭合字形时吞掉末位 0；用真实地下室 24050 的源路径加入回归。
- 连通分析后释放空间索引，按方向回放临时路径并跳过无效候选对象，减少大图内存峰值；临时文件计入现有磁盘限额。
- 同一投影尺寸去重，防止重复标注同时进入比例拟合和留出验证。
- 同一投影跨度的不同标签均排除出拟合，并保留冲突记录与独立检查。
- 交付验证重算独立两轴证据及总分尺寸链，核验输出尺寸实体、端点和标签，拒绝缺失或不一致的证据。
- 修复文本去重的坐标/字高归一化碰撞；仅隐藏属性完全一致的重复文本，保留原始字符及实体。
- 保留 PDF 原始坐标与 0.2% 尺寸门槛；不把量化误差改成名义尺寸，不推测中文轮廓或多比例视口。

# 2.0.0rc6

- 修复 NumPy 2.5 不再支持二维 cross 输入导致真实曲线转换失败。

- 在持久化 DXF 上补齐有限数字模板识别、原生文字分组和尺寸界线配对。
- 修复缩短尺寸线造成比例偏差，以及 C1564 等构件编号被截成尺寸数字的问题。
- 用横竖独立尺寸、留出数据和总分尺寸链确认工程比例，真实换算 DXF 坐标及输出单位。
- 为每条证据保留源 handles、端点、标签、换算值和误差；保留不达标的短尺寸误差，不修改标注来通过检查。
- 多比例、单轴或冲突证据保留纸面结果。矢量字形模板有来源记录；不再依赖缺失的项目字典，未识别文字不补猜。

# 2.0.0rc5

- 统一版本号，声明运行依赖，OCR 包延迟导入，保留原始归档 SHA-256。
- 识别纸面回退、无尺寸证据、缺字形资源和图片提取错误；分别报告几何有效性、比例、建模就绪状态。
- 实现单位和手动全页比例；传递严格模式和原轮廓处理策略；拒绝不支持的参数值。
- 仅归一化指定 DXF 元数据，不重写图纸日期、UUID、负零文字、路径或 XDATA。
- 移除错误的跨任务输出缓存，避免旧 DXF 复用和并发写入竞态；确定性由两次完整计算验证。
- 图片内容寻址、相对路径重写、资源优先发布、独立工作目录和同名输入隔离。
- 逐条提取、文件暂存并复用 MuPDF 图元；保持原有 fill/stroke 合并、裁剪和 Form 顺序。
- 无 DIMENSION 且字形恢复关闭时，跳过必然为空的标定和重复后处理；及时释放 DXF 文档。
- 监控整页进程树的 RSS、超时和临时磁盘，终止子孙进程并保存结构化失败报告。
- 修复无效页码假成功及报告 artifacts 自引用；恢复 R12 隐藏/冻结图层、字体样式和曲线填充边界。
- 移除原 rc2 强制正交化补丁，保留测得的仿射坐标轴。原三视口基准尺寸闭合恢复。
- 补上 rc4 测试调用的显式 DIMLFAC 辅助函数，限制调整范围并保存原标签和误差；稳定转换不自动调用，验证仍检查原几何误差。

字形字典未随原包交付，仍不可恢复原本不存在的文字；未确认的真实比例仍须保留为未知。

- 按职责整理内部命名空间，合并旧格式验证器；删除旧转换/批处理/渲染入口、旧仿射导出器、未使用的 PDFium/API、验收试验脚本和重复字形流程。移除 sys.path/PYTHONPATH 注入和通配顶层打包，只保留一个 CLI。

- PDF texttrace 返回替换符时，仅以同字体、同坐标、唯一候选的原生 rawdict Unicode 补全；不移动字符、不调用 OCR、不补猜测内容。多页实图第 7 页两处字符已补全。
